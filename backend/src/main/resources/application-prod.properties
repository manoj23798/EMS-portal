# Server Configuration
server.port=${PORT:8080}

# Database Configuration (Neon.tech PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-sparkling-bonus-am0n47mt-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USER:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_QaJtRASH65Vl}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate settings (Create tables automatically but don't drop them)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (Load from environment variables for security)
jwt.secret=${JWT_SECRET_KEY:8z2Xb9Qp4Wm6Yv3Rt1Kc7Jd5Hn8Fq6Zs2Xb9Qp4Wm6Yv3Rt1Kc7Jd5Hn}
jwt.expiration=${JWT_EXPIRATION:86400000}

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# CORS settings
app.cors.allowed-origins=${FRONTEND_URL:*}
